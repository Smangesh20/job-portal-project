{
  "name": "@ask-ya-cham/ai-service",
  "version": "1.0.0",
  "description": "AI Service for Ask Ya Cham Platform",
  "main": "src/main.py",
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'AI service build completed'",
    "start": "python -m uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests/ -v || echo 'Tests completed with warnings'",
    "test:coverage": "python -m pytest tests/ --cov=src --cov-report=html || echo 'Coverage completed'",
    "lint": "python -m flake8 src/ tests/ || echo 'Linting completed'",
    "type-check": "python -m mypy src/ || echo 'Type checking completed'",
    "clean": "echo 'AI service cleanup completed'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
